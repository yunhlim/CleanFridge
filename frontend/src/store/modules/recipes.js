// import router from '@/router'
// import axios from 'axios'
// import drf from '@/api/drf'


export default {
    state:{
        recipe: {},
        recipes: [],
    },
    getters:{
        recipe: state => state.recipe,
        recipes: state => state.recipes,
    },
    mutations:{

    },
    actions:{
        // fetchRecipes({ page, query, isRecommend, type })
        // fetchRecipe({ RecipePk })
        // likeRecipe({ RecipePk })
    }
}